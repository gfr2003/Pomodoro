import { TimerDirectiveDirective } from './timer-directive.directive';

describe('TimerDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TimerDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
